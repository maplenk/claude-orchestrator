# Design source

Mockups exported from the Claude Design project. `.dc.html` files are canvas mockups —
they use `sc-if` / `sc-for` templating and a `support.js` runtime that are NOT shipped.
The implementation in `ui/dashboard.html` is the real, data-wired version.

See github.md for the sync log and the design decisions behind v2.
